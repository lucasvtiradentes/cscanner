{
  "$schema": "https://turbo.build/schema.json",
  "remoteCache": {
    "enabled": true
  },
  "tasks": {
    "typecheck": {
      "outputLogs": "new-only",
      "inputs": [
        "./src/**",
        "./*.config.*",
        "./tsconfig.json",
        "./package.json"
      ]
    },
    "build": {
      "outputLogs": "new-only",
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "out/**", "target/release/**", "binaries/**"],
      "inputs": [
        "src/**",
        "crates/**",
        "scripts/**",
        "*.config.*",
        "tsconfig.json",
        "Cargo.toml",
        "Cargo.lock",
        "package.json"
      ]
    },
    "build:rust": {
      "outputLogs": "new-only",
      "outputs": [
        "target/release/cscanner",
        "target/release/cscanner.exe",
        "target/release/cscanner-server",
        "target/release/cscanner-server.exe"
      ],
      "inputs": [
        "src/**",
        "crates/**",
        "Cargo.toml",
        "Cargo.lock"
      ]
    },
    "core#build": {
      "outputLogs": "new-only",
      "outputs": [
        "target/release/**"
      ]
    },
    "cscanner#build": {
      "outputLogs": "new-only",
      "dependsOn": ["core#build"],
      "outputs": ["dist/**", "binaries/**"],
      "inputs": ["./src/**", "./scripts/**", "./tsconfig.json", "./package.json", "../core/target/release/**"]
    },
    "cscanner-vscode#build": {
      "outputLogs": "new-only",
      "dependsOn": ["core#build"],
      "outputs": ["out/**", "binaries/**"],
      "inputs": ["./src/**", "./esbuild.config.ts", "./package.json", "../core/target/release/**"]
    },
    "test": {
      "outputLogs": "new-only",
      "inputs": [
        "./src/**",
        "./crates/**",
        "./Cargo.toml",
        "./Cargo.lock"
      ]
    },
    "clippy": {
      "outputLogs": "new-only",
      "inputs": [
        "./src/**",
        "./crates/**",
        "./Cargo.toml",
        "./Cargo.lock"
      ]
    },
    "format": {
      "outputLogs": "new-only",
      "inputs": ["./src/**", "./crates/**", "./biome.json", "./rustfmt.toml"]
    },
    "format:ts": {
      "outputLogs": "new-only",
      "inputs": ["./src/**", "./biome.json"]
    },
    "format:rust": {
      "outputLogs": "new-only",
      "inputs": ["./crates/**", "./src/**", "./rustfmt.toml"]
    },
    "format:check": {
      "outputLogs": "new-only",
      "inputs": ["./src/**", "./crates/**", "./biome.json", "./rustfmt.toml"]
    },
    "lint": {
      "outputLogs": "new-only",
      "inputs": ["./src/**", "./crates/**", "./Cargo.toml", "./biome.json"]
    },
    "lint:fix": {
      "outputLogs": "new-only",
      "inputs": ["./src/**", "./crates/**", "./Cargo.toml", "./biome.json"]
    }
  }
}
